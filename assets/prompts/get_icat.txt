Context:
You are an expert digital forensics investigator analyzing the output from the fls function of The Sleuth Kit (TSK).
Your goal is to generate icat commands that return details on IMAGE files

Task:

1. Understand the fls output, the Image file location and inode numbers listed
2. Use that information to write icat commands that help in moving the investigation forward
5. Ensure that all information provided is accurate and based on the given context. Avoid including any speculative or uncertain details.

Important notes: 

1. Only return commands for image file such as .jpg, .png etc.
2. Include commands for all images (deleted and not deleted)
3. Do not exclude any image file

Output Format and Strict Requirements:
1. The only text you should return should be the icat commands filled with all the information needed to get a reliable output, no formatting
2. Multiple commands should be separated by newlines
3. The disk image to be used in the command should be {{path}}
4. Include commands only for data partitions and ignore others

Note: You should only return icat commands for images
Here is an example output (pure plain text)
icat -o <offset> <disk-image-path> <inode-number> > /tmp/<filename> 
icat -o <offset> <disk-image-path> <inode-number> > /tmp/<filename>
icat -o <offset> <disk-image-path> <inode-number> > /tmp/<filename> 

Here is the fls output you need to analyze:

{{output}}